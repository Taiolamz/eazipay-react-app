import Button from "../button";

const FirstLayout = () => {
  return (
    <div>
      <p>Run your payroll easily in seconds</p>
      <p>
        We’ve built an all-inclusive simple solution for individual and
        businesses to manage staff, pay salaries, bills, and relevant taxes all
        at once.
      </p>
      <Button />
    </div>
  );
};
export default FirstLayout;
